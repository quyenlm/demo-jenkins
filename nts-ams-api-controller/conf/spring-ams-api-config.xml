<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:aop="http://www.springframework.org/schema/aop"
	xmlns:tx="http://www.springframework.org/schema/tx" xmlns:jee="http://www.springframework.org/schema/jee"
	xsi:schemaLocation="http://www.springframework.org/schema/beans
	http://www.springframework.org/schema/beans/spring-beans-3.1.xsd
    http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx-3.1.xsd
    http://www.springframework.org/schema/aop http://www.springframework.org/schema/aop/spring-aop-3.1.xsd
    http://www.springframework.org/schema/jee http://www.springframework.org/schema/jee/spring-jee-3.1.xsd">
	
	<bean
		class="org.springframework.beans.factory.config.PropertyPlaceholderConfigurer">
		<property name="locations">
        	<list>
				<value>spring-server-config.properties</value>
				<value>configs.properties</value>
			 </list>
   	  	</property>
	</bean>
	
	<!-- AMS-API messages config -->
	<bean id="errMsgs" class="org.springframework.beans.factory.config.PropertiesFactoryBean">
	  <property name="locations">
        <list>
            <value>err-be.properties</value>
            <value>err-fe.properties</value>
        </list>
   	  </property>
	</bean>
	
	<!-- AMS-API config -->
	<bean id="amsConfiguration" class="com.nts.ams.api.controller.setting.Configuration">
		<property name="amsApiMode" value="${api.mode}" />
		<property name="amsApiProtoVersion" value="${api.amsApiProtoVersion}" />
		<property name="maxRequestOnTime" value="${api.maxRequestOnTime}" />
		
		<property name="maxTimeCacheAgreement" value="${api.maxTimeCacheAgreement}" />
		<property name="minTimeCacheAgreement" value="${api.minTimeCacheAgreement}" />
		
		<property name="profilePoolSize" value="${api.profilePoolSize}" />
		<property name="profileUpdatePoolSize" value="${api.profileUpdatePoolSize}" />
		<property name="balanacePoolSize" value="${api.balanacePoolSize}" />
		
		<property name="agreementPoolSize" value="${api.agreementPoolSize}" />
		<property name="transferPoolSize" value="${api.transferPoolSize}" />
		<property name="depositPoolSize" value="${api.depositPoolSize}" />
		<property name="withdrawalPoolSize" value="${api.withdrawalPoolSize}" />
				
		<property name="stopServiceTimeOut" value="${api.stopServiceTimeOut}" />
		<property name="balanaceTimeOut" value="${api.balanaceTimeOut}" />
		
		<property name="mypageUrl" value="${mypage.url}" />
		<property name="mypageAvataUrl" value="${mypage.avata.url}" />
		
		<property name="sendAmsOffSignalNotificationMail" value="${api.sc.sendOffSignalNotificationMail}" />
	</bean>
</beans>